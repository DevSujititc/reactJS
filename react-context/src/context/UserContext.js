import  {createContext} from "react";

const userContext = createContext();

export default userContext;

// Next way to use createContext
// import React from "react";

// const userContext = React.createContext();

// export default userContext

